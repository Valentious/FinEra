
  # User Registration Flow

  This is a code bundle for User Registration Flow. The original project is available at https://www.figma.com/design/7JnkaymnSfNwWQY5c9NEWu/User-Registration-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  