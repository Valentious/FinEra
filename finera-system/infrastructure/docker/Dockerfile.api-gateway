# FinEra API Gateway - Dockerfile placeholder
FROM node:20-alpine
WORKDIR /app
COPY backend/api-gateway/package*.json ./
RUN npm ci --only=production
COPY backend/api-gateway/dist ./dist
EXPOSE 4000
CMD ["node", "dist/index.js"]
